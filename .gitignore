__pycache__/
*.py[cod]
*.egg-info/
.venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/
dist/
build/

# Runtime DB (created on first ingest; see section-01 spec)
lodestone.db
lodestone.db-*

# Smoke-test artifacts
/tmp/lodestone_*.db

# Claude Code per-user state
.claude/settings.local.json
.claude/scheduled_tasks.lock

# Project-local MCP server config (user-specific absolute paths).
# Use .mcp.json.example as the shared template; copy to .mcp.json
# locally and edit. NOT shipped via the plugin marketplace install.
.mcp.json
