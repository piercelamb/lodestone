__pycache__/
*.py[cod]
*.egg-info/
.venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/
dist/
build/

# Runtime DB (created on first ingest; see section-01 spec)
lodestone.db
lodestone.db-*

# Smoke-test artifacts
/tmp/lodestone_*.db

# Claude Code per-user state
.claude/settings.local.json
.claude/scheduled_tasks.lock

# Project-local MCP server config for dev work (user-specific absolute paths).
# Use .mcp.dev.json.example as the shared template; copy to .mcp.dev.json
# locally and edit. NOT shipped via the plugin marketplace install.
# (The tracked .mcp.json at the repo root is the plugin's MCP entrypoint —
# it lives under the plugin's CLAUDE_PLUGIN_ROOT, not in the dev workflow.)
.mcp.dev.json
